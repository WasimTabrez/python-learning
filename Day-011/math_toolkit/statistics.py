def maximum(numbers):
    return max(numbers)


def minimum(numbers):
    return min(numbers)


def total(numbers):
    return sum(numbers)


def average(numbers):
    return sum(numbers) / len(numbers)
